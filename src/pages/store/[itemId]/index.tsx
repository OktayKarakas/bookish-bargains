import React from "react";
import { useRouter } from "next/router";

const Item = () => {
  const router = useRouter();
  return (
    <div>
      <h1>SingleItem</h1>
    </div>
  );
};

export default Item;
